import UIKit

enum AppColors {

    // MARK: - Backgrounds

    static let background = UIColor(named: "Background")!
    static let card = UIColor(named: "Card")!

    // MARK: - Text Colors

    static let foreground = UIColor(named: "Foreground")
    static let mutedForeground = UIColor(named: "MutedForeground")

    // MARK: - Primary

    static let primary = UIColor(named: "Primary")
    static let primaryForeground = UIColor(named: "PrimaryForeground")

    // MARK: - Secondary

    static let secondary = UIColor(named: "Secondary")
    static let secondaryForeground = UIColor(named: "SecondaryForeground")

    // MARK: - Accent

    static let accent = UIColor(named: "Accent")
    static let accentForeground = UIColor(named: "AccentForeground")

    // MARK: - Muted

    static let muted = UIColor(named: "Muted")

    // MARK: - Border

    static let border = UIColor(named: "Border")!

    // MARK: - Destructive

    static let destructive = UIColor(named: "Destructive")
    static let destructiveForeground = UIColor(named: "DestructiveForeground")
}
